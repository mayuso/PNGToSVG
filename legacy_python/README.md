## Legacy Python
The original Python implementation is kept here for educational purposes, but it is no longer actively maintained. To use this legacy version:

1. Install dependencies: `pip install -r requirements.txt`
2. Run: `python main.py`